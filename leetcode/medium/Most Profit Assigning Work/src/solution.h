#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

int maxProfitAssignment( const int* difficulty, int difficultySize, const int* profit, int profitSize, const int* worker, int workerSize );
